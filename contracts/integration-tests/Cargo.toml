[package]
name = "integration-tests"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["rlib"]

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
swap-book = { path = "../swap-book" }
router = { path = "../router" }
twap-book = { path = "../twap-book" }
fee-vault = { path = "../fee-vault" }
